# BUILD BACKLOG

Status values: VERIFIED_COMPLETE, PARTIAL, NEEDS_INTEGRATION, NEEDS_HARDENING, NEEDS_TESTS, NEEDS_FRONTEND, NEEDS_E2E, BLOCKED_EXTERNAL, UNKNOWN.

| ID | Domain | Description | Current State | Dependency | Severity | Business Impact | Priority | Verification Required |
|---|---|---|---|---|---|---|---|---|
| BL-01 | Tenancy | Per-model organization scoping audit — CLOSED 2026-09-12: original "4/26+" finding was a grep artifact (org_id inherited via `TenantScopedMixin`, not redeclared per file); 22/24 domain models confirmed to use the single canonical mixin. Real gap found instead: 7 `create_*` service functions accepted unchecked cross-tenant nested FKs (IDOR) | FIXED — see docs/FULL_SYSTEM_AUDIT.md §14/§22 for full detail and file:function list | none | Critical (was) | Multi-tenant data leakage if incomplete — now closed for the 7 proven paths | Done | `backend/tests/integration/test_tenancy_isolation.py` (9 tests, all passing); ruff/mypy/pytest full suite green (356/356) |
| BL-01a | Tenancy | Read/update/delete paths for technicians, AOG, maintenance, inventory, parts, vendors, compliance, regulatory, receiving lack dedicated cross-tenant regression tests (enforcement pattern present, but unverified by test this pass) | PARTIAL | BL-01 | Medium | Same class of bug as BL-01 could exist on read/update/delete, unconfirmed | P1 | Extend test_tenancy_isolation.py with GET/PATCH/DELETE cases per remaining domain |
| BL-01b | Tenancy | `receiving_service.py` not audited for nested-FK IDOR this pass | UNKNOWN | BL-01 | Medium | Same class of bug as BL-01 could exist, unconfirmed | P1 | Read receiving_service.py create/update paths for unchecked FK params |
| BL-01c | Tenancy/DB | `organization_id` columns are application-enforced only — no DB-level FK to `organization.id`, no composite constraint | PARTIAL (safe today via service layer, no defense-in-depth) | none | Low-Medium | A future service-layer bug would have no DB-level backstop | P2 | Product/eng decision: add FK constraints via migration (needs a data audit first for any pre-existing orphans) |
| BL-01d | Tenancy/Docs | `TenantScopedMixin` docstring claims `RegulatoryDocument` is intentionally global reference data, but the model inherits the mixin and is actually org-scoped | PARTIAL (not a vulnerability — safer-than-documented state) | none | Low | Confusing to future engineers; may indicate an unresolved product question (should regulatory docs be shared or per-org?) | P2 | Product decision, then fix either the docstring or the model to match intent |
| BL-02 | Frontend/Evidence | CONFIRMED 2026-09-12 (re-verified, no code change needed — REAL-mode wiring already existed from a prior session, commit `3dd86fa`): `frontend/lib/api/evidence.ts` (typed client for `backend/app/api/v1/evidence.py`: create/get/transition) and `frontend/components/evidence/RealTaskGatePanel.tsx` (per-task panel wired into `maintenance/work-orders/[id]` REAL mode) exist and work end-to-end against the live backend. The dedicated `app/(app)/evidence` list/detail pages remain DEMO-only by design — they model a different concept (assessment-linked document evidence with no backend equivalent) from the backend `Evidence` model (per-task lifecycle/status record with no file-upload field at all — `apiUploadFile` exists in `apiClient.ts` but there is no evidence upload endpoint to call it against). No unified completion-gate endpoint exists; the panel shows Evidence and Inspection as two honestly-labeled independent signals rather than synthesizing one. | VERIFIED_COMPLETE (for the task-scoped REAL-mode lifecycle panel; the standalone `/evidence` mock pages are out of scope — no backend equivalent to wire them to) | none | — | Evidence create/submit/review/accept/reject usable end-to-end against the real backend from the work-order detail page | Done | Backend: `pytest -k evidence` 17/17 green. Frontend: `npm run lint`/`npm run typecheck` clean (pre-existing unrelated warnings only); `npm run build` intermittently fails on Windows with `ENOENT .next/...manifest` (stale build cache under this sandbox's file locking — reproduced 3x, unrelated to evidence code, compile+typecheck+all 61 static pages consistently succeed before the failure). Live-browser REAL-mode check 2026-09-12: registered a fresh org, created aircraft/work-order/task via API, clicked "Create evidence record" in the UI → UPLOADED → Submit → SUBMITTED; full remaining lifecycle (→ AWAITING_REVIEW → REJECTED, then confirmed REJECTED→ACCEPTED is correctly refused by the state machine) verified via direct API calls exercising the same `evidence_service.transition_evidence` path the UI calls, after a concurrent session sharing this sandbox's browser repeatedly interrupted the UI click-through (unrelated environment issue, not a code defect). DEMO mode confirmed unaffected. |
| BL-03 | Frontend/Inspections | CLOSED 2026-09-12: `frontend/lib/api/inspections.ts` client added; `maintenance/inspections` list and `[id]` detail pages now branch REAL/DEMO via `useDataMode`, calling `/inspections` create/get/list-by-work-order/transition. RII independence rule is enforced backend-side only (`inspection_service._assert_independent_inspector`) — frontend surfaces the resulting error, never re-derives it. No bulk "all requirements" endpoint exists, so the REAL list fans out per work order (documented in-code as a prototype-scale limitation) | DONE — see docs/ROADMAP_STATUS.md `inspections` row | none | High | Inspection workflow now usable against the real backend | Done | Backend: 15 inspection tests pass (`pytest -k inspection`). Frontend: lint/typecheck/build clean; browser-verified REAL-mode empty-state list, DEMO-mode unaffected, 375x812 mobile layout; live create/transition against seeded data not confirmed this pass because the shared dev DB was emptied by other agents' concurrent work mid-session |
| BL-04 | Frontend/Deferred-MEL | CLOSED 2026-09-12: `frontend/app/(app)/maintenance/deferred/page.tsx` existed but was DEMO-only (no `useDataMode` branch). Added a REAL-mode branch backed by `frontend/lib/api/deferred-items.ts` (typed client for `backend/app/api/v1/deferred_items.py`) — lists fleet deferred items, buckets ACTIVE/DUE_SOON/OVERDUE/CLOSED by a same-day comparison against the backend's own `due_at`/`status` fields (no invented compliance signal), and supports the closure workflow gated entirely by the backend (`deferred_item_service.close_deferred_item`: blocked until `approval_status == APPROVED` when `approval_required`). DEMO mode untouched. | VERIFIED_COMPLETE (frontend build/lint/typecheck green; backend `pytest -k deferred` 12/12 green; live-browser REAL-mode check confirmed correct bucketing and approval-gated closure UI — see PR/commit for screenshots) | backend deferred_items API (exists) | — | — | Done | `frontend/lib/api/deferred-items.ts`, `frontend/app/(app)/maintenance/deferred/page.tsx` |
| BL-05 | Frontend/Compliance | Compliance page mock status unconfirmed | DONE 2026-09-12 — `frontend/app/(app)/compliance/page.tsx` and `frontend/app/(app)/compliance/regulatory-register/page.tsx` now branch on `useDataMode`/`isReal` and call the real backend (`frontend/lib/api/compliance.ts`, new) for regulatory requirements and compliance assessments. DEMO branch unchanged. `pre-audit/page.tsx` left MOCK-only (out of scope — it composes evidence/inspections/parts data owned by other in-flight work) | lib/api compliance client (added) | High | Compliance chain now usable end-to-end in UI for the two wired pages; pre-audit dossier remains demo-only | Done | Live-verified in REAL mode against a seeded org (regulatory requirements + assessments created via API/DB) and in DEMO mode (unaffected); `npm run lint`, `npm run typecheck`, `npm run build` all pass; backend `pytest -q` 356/356 (no backend changes made) |
| BL-06 | Frontend/Release-Readiness | CLOSED 2026-09-12: `frontend/lib/api/release-readiness.ts` (typed client for `GET /work-orders/{id}/release-readiness`) added; new `frontend/components/evidence/RealReleaseReadinessPanel.tsx` wired into `maintenance/work-orders/[id]` REAL mode, showing READY/BLOCKED status and each blocker's category/description/related_record_id exactly as returned by `release_readiness_service` (EVIDENCE, INSPECTION, TASK_EXECUTION only — no client-side readiness calculation) | VERIFIED_COMPLETE (frontend build/lint/typecheck green; backend `pytest -k release` 18/18 green; live-browser trace confirmed BLOCKED→3 blockers→2 blockers transitions reflected with no stale cache, both via in-page Refresh and full navigate-away/back) | backend release_readiness API (exists) | — | Release readiness now visible on the real work-order detail page | Done | See BL-16/BL-17 below for gaps surfaced during this verification |
| BL-16 | Backend/Tasks | CLOSED 2026-09-13: added `POST /work-orders/{work_order_id}/tasks/{task_id}/complete` (`backend/app/api/v1/work_orders.py`) backed by `work_order_service.complete_task` — org-scoped lookup, rejects already-COMPLETED and any state outside `{PENDING, IN_PROGRESS}`, records a `task.completed` audit event, never touches Evidence/Inspection gates (task completion and release readiness stay separate checks). Frontend: `tasksApi.complete` (`frontend/lib/api/tasks.ts`) plus a "Complete Task" button on `maintenance/work-orders/[id]` that refetches tasks and remounts `RealReleaseReadinessPanel` (key bump) so readiness never goes stale. | VERIFIED_COMPLETE | none | — | A work order's TASK_EXECUTION blocker can now be cleared through the product, enabling a genuine READY path | Done | `backend/tests/integration/test_task_completion_api.py` (5/5: valid completion, duplicate rejected 409, cross-tenant rejected 404, audit event recorded, readiness blocker clears); full backend suite 366/366; ruff/mypy clean; frontend lint/typecheck/build clean; live-browser REAL-mode check (see BL-06/full-chain note below) confirmed PENDING→button click→COMPLETED badge and BLOCKED→READY on the same page with no manual reload |
| BL-17 | Backend+Frontend/Inspections | CLOSED 2026-09-13: root cause was `inspection_service.transition_inspection_requirement` falling back to the requirement's stale prior `inspector_user_id` (usually `None`) instead of the authenticated actor when the request omitted `inspector_user_id` — so "Complete (as me)" always sent `null` and always failed RII's "inspector_user_id is required" check. Fixed by deriving the inspector server-side from `actor_user_id` when the field is omitted (matching how `organization_id` is always server-derived), while still honoring an explicitly supplied `inspector_user_id` for the supervisor-on-behalf-of case (the inspections list-page's inspector dropdown) — now validated against the caller's own organization (`_assert_user_in_organization`) to close a latent cross-tenant IDOR that existed in the old unchecked client-supplied field. The RII independence rule itself (`_assert_independent_inspector`) is unchanged. Frontend: `RealTaskGatePanel`'s "Complete (as me)" now passes the session user's id explicitly (server default would suffice too; explicit is clearer) instead of `undefined`. | VERIFIED_COMPLETE | none | — | RII inspections can now be completed end-to-end through both the "Complete (as me)" button and the supervisor inspector-picker, clearing the INSPECTION blocker | Done | `backend/tests/integration/test_inspection_completion_identity_api.py` (4/4: complete-as-me succeeds and records the actor, explicit inspector from another org rejected 409, independence violation still rejected 409 for both explicit-self and omitted-id forms, independent-inspector completion succeeds + audit event + readiness clears); full backend suite 366/366; ruff/mypy clean; frontend lint/typecheck/build clean; live-browser REAL-mode check: created an RII requirement, clicked "Complete (as me)" — previously always failed, now shows COMPLETED with no error |
| BL-07 | Backend Tests | No confirmed API-level integration test for work_orders | PARTIAL (service exists, router exists, no dedicated api test file found) | none | Medium | Regression risk on core workflow | P2 | Search all integration tests for indirect work_orders API coverage |
| BL-08 | Backend Tests | No confirmed API-level test for evidence, inspections, deferred_items, vendor_part_availability | PARTIAL (unit/service tests exist for evidence/inspections) | none | Medium | Regression risk | P2 | Add/confirm API-level test files |
| BL-09 | Compliance Engine | Regulatory applicability condition-tree evaluation explicitly NOT backend-resident (self-documented in tools.py header) | PARTIAL (data model exists via regulatory_service/compliance_service, evaluation logic absent) | regulatory_service, compliance_service | High | Any UI/Lisa claim of automated applicability determination would be false | P1 | Confirm no code path fabricates applicability answers; scope real engine design |
| BL-10 | Lisa Frontend Wiring | Whether frontend/app/(app)/ai/ calls real /api/v1/lisa endpoint end-to-end unconfirmed | UNKNOWN | backend lisa.py (exists, 73 lines) | Medium | AI assistant may be partially mocked in UI | P1 | Live browser trace of a Lisa query end-to-end |
| BL-11 | Mobile | No breakpoint/media-query audit performed | UNKNOWN | none | Low-Medium | Unknown mobile usability | P3 | Grep frontend CSS/Tailwind config for responsive breakpoints; manual device test |
| BL-12 | Cross-engine duplication | Frontend pages not checked for client-side status/eligibility logic duplicating backend services | UNKNOWN | none | Medium | Risk of UI/backend disagreement on computed status (maintenance-due, release-readiness, etc.) | P2 | Grep the ~13 REAL-mode pages for local derivation of status fields already computed server-side |
| BL-13 | Migrations | Column-level FK/nullable/enum/unique review not performed for all 24 migrations | UNKNOWN | none | Medium | Schema correctness gaps may be hidden | P2 | Read each migration file, cross-check against model definitions |
| BL-14 | Aircraft/Work-Orders/Technicians/Control-Center/AOG-Recovery/Parts/Purchase-Orders/Assessment-Intelligence/Settings/Login frontend | Confirmed REAL-mode wiring present | VERIFIED (structurally, via useDataMode/isReal grep) | none | n/a | Already delivers real data | n/a — do not rebuild | Optional: live browser smoke test to reach BROWSER_VERIFIED |
| BL-15 | Lisa tool-to-service delegation | Confirmed all Lisa tools call canonical services, permission-gated, org_id from auth context only | VERIFIED_COMPLETE (code read directly, tools.py lines 1-90) | none | n/a | Strong architectural asset | n/a | None further needed for this specific claim |
| BL-18 | Mobile/Layout | Observed live 2026-09-13 while browser-verifying BL-16/BL-17 at 375x812: the app-wide left sidebar nav does not collapse/hide on narrow viewports and overlaps the main content column on `maintenance/work-orders/[id]` (and, by construction, every other page using the same shell layout) — pre-existing, not introduced by BL-16/BL-17 | CONFIRMED GAP (not fixed — out of scope; this pass only touched task/inspection completion components) | none | Medium | Any REAL-mode page is hard to use below ~768px width | P2 | Add a responsive collapse/hamburger behavior to the shared app shell/sidebar component (not yet located this pass) and re-verify at 375x812 |
| BL-19 | Frontend/Evidence+Inspection panel refresh | `RealTaskGatePanel`'s own Evidence/Inspection transitions do not trigger a refresh of the sibling `RealReleaseReadinessPanel` on `maintenance/work-orders/[id]` — only the new BL-16 "Complete Task" button does (via a key-bump). An evidence/inspection state change still requires the readiness panel's own manual "Refresh" button. This is a narrower instance of the same "no unified completion gate" limitation the panel already documents in its own header text. | PARTIAL (task-completion path fixed this pass; evidence/inspection paths still require manual Refresh) | none | Low | Minor UX staleness, not a correctness bug — the manual Refresh button always shows the true backend state | P3 | Thread the same `readinessRefreshKey` bump (or a shared callback) through `RealTaskGatePanel`'s evidence/inspection transition handlers |
